[tool.isort]
profile = "black"
known_first_party = ["project_delta", "utils"]
src_paths = ["."]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]  # E/F = pyflakes/pycodestyle, I = import sorting

[tool.pytest.ini_options]
testpaths = ["tests"]
