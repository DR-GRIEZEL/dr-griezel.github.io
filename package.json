{
  "name": "dashboard-widgets",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --config test/vitest.config.js",
    "test:watch": "vitest --config test/vitest.config.js",
    "coverage": "vitest run --config test/vitest.config.js --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0",
    "vitest": "^1.6.0"
  }
}
